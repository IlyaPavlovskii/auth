<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.github.ilyapavlovskii.kmm.auth.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="io.github.ilyapavlovskii.kmm.auth.sample.AuthApplication"
        android:forceDarkAllowed="true"
        android:theme="@style/Theme.AuthThemeSample"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="tiramisu"
        >
        <activity
            android:name="io.github.ilyapavlovskii.kmm.auth.sample.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AuthThemeSample"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>